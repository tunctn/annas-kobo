#!/bin/sh
# Cross-compile the device binary (static musl, armv7).
set -eu
cd "$(dirname "$0")/.."
export PATH=/opt/homebrew/opt/rustup/bin:/opt/homebrew/bin:$PATH
cargo zigbuild --release --target armv7-unknown-linux-musleabihf
ls -la target/armv7-unknown-linux-musleabihf/release/annas-kobo
